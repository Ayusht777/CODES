// BASIC OF STRING Initializing a String
// syntax : char str_name[size];
#include<stdio.h>
int main()
{
         char x[5]="ayush";// their is not a \o null character because group of char without , doesn't required null
         printf("\n%s",x);// printing it with printf
}

